{
  "buildCommand": "node scripts/swap-provider.js postgresql && npx prisma generate && npx prisma db push --accept-data-loss && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}